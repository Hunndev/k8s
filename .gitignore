# ───────────────────────────────────────────────────────────────
# OS / editor
# ───────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
*.swp
*.swo
*~
.vscode/
.idea/

# ───────────────────────────────────────────────────────────────
# 사이트 데이터 — 실고객사 정보 차단 (allow-list 방식)
# ───────────────────────────────────────────────────────────────
# 기본: /sites/ 안의 모든 것을 재귀적으로 차단
/sites/**

# 단, 다음만 git에 포함 (명시적 재허용):
!/sites/SCHEMA.md
!/sites/CLAUDE.md
!/sites/_TEMPLATE/
!/sites/_TEMPLATE/**
!/sites/sample-customer/
!/sites/sample-customer/**

# 새 사이트(/k8s-survey로 등록)는 자동으로 차단됨.
# 의도적으로 시연용 사이트를 추가하려면 위에 ! 라인 두 줄 추가 필요
# (디렉토리 + 내부 파일 둘 다 해제해야 git이 들어감):
#   !/sites/<이름>/
#   !/sites/<이름>/**

# ───────────────────────────────────────────────────────────────
# oh-my-claudecode 세션 데이터
# ───────────────────────────────────────────────────────────────
.omc/

# ───────────────────────────────────────────────────────────────
# 테스트 임시 파일
# ───────────────────────────────────────────────────────────────
tests/.bats-tmp/
tests/.tmp/

# ───────────────────────────────────────────────────────────────
# 백업 파일
# ───────────────────────────────────────────────────────────────
*.bak
*.bak-*
